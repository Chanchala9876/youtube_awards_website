<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Creator Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .creator-img {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 50%;
      border: 3px solid #fff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }
    .creator-card {
      transition: transform 0.2s;
    }
    .creator-card:hover {
      transform: translateY(-5px);
    }
    footer {
      background-color: #f8f9fa;
      padding: 20px 0;
      text-align: center;
      border-top: 1px solid #ddd;
      margin-top: 60px;
    }
  </style>
</head>
<body class="bg-light">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
      <a class="navbar-brand" href="#">The Awards</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link" href="/awards">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="#">My Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Header -->
  <header class="bg-primary text-white text-center py-5 mb-4">
    <div class="container">
      <h1 class="display-5">Welcome, <%= creator.name %></h1>
      <p class="lead mb-0">Field: <strong><%= creator.field_name %></strong></p>
    </div>
  </header>

  <!-- Leading Creator -->
  <div class="container mb-4">
    <div class="alert alert-info text-center shadow-sm">
      🌟 <strong>Leading Creator:</strong> <%= leadingCreator.name %> with <%= leadingCreator.votes_count %> votes
    </div>
  </div>

  <!-- All Creators -->
  <div class="container">
    <h3 class="mb-4 text-center">All Creators in Your Field</h3>
    <div class="row">
      <% fieldCreators.forEach(c => { %>
        <div class="col-md-4 col-sm-6 mb-4">
          <div class="card creator-card p-3 shadow-sm">
            <div class="text-center">
              
               <img src="<%= creator.photo_path %>" alt="<%= creator.name %>">
              <h5 class="card-title"><%= c.name %></h5>
              <p class="text-muted"><%= c.channel_name %></p>
            </div>
            <ul class="list-group list-group-flush">
              <li class="list-group-item">Subscribers: <%= c.subscriber_count %></li>
              <li class="list-group-item">Votes: <%= c.votes_count %></li>
            </ul>
            <div class="card-body text-center">
              <form action="/vote" method="POST">
                <input type="hidden" name="creator_id" value="<%= c.creator_id %>">
                <input type="hidden" name="field_id" value="<%= c.field_id %>">
                <!--<button type="submit" class="btn btn-outline-primary mt-2">Vote for <%= c.name %></button>-->
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <!-- Footer -->
  <footer class="mt-5">
    <div class="container">
      <p class="mb-0">&copy; <%= new Date().getFullYear() %> The Awards. All rights reserved.</p>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
